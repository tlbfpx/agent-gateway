apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-gateway.fullname" . }}
  labels: {{- include "agent-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "agent-gateway.selectorLabels" . | nindent 4 }}